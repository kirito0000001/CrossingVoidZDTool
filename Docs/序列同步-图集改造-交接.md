# 序列同步改为图集导入 · 进度与交接

> 写于 2026-09-15。第五步「序列同步」从「逐帧导入 PNG」改成「导入一张图集 + 按矩形切精灵」。
> 这份文档是给下一个接手的人（或上下文压缩之后的自己）看的，只记**事实和证据**，不写推测当结论。

---

## 一、这次要解决什么

原来的链路是：每张 PNG 各导入成一个 Texture，再各建一个 Sprite，最后用这些 Sprite 建 Flipbook。
复用位置会被各导一份，于是 Sk2 素材目录里 17 张图，导入后变成 23 张贴图加 23 个精灵。

改成：一张图集贴图 + 每个素材一个 Sprite，复用位置在 Flipbook 的关键帧层面共享同一个 Sprite。

顺带明确了排序权威：**帧序由工具箱的 `sequence.json` 决定**（含复用与空白帧），
图集的 `<图集名>_sequence.json` 只回答「每张素材在图集里的矩形」，不决定顺序。

---

## 二、已完成

### 交付层（C#）

| 加了什么 | 位置 |
|---|---|
| 图集帧表的模型（`AtlasSequenceManifest` / `AtlasSequenceFrame` / `AtlasRect`） | `Models/AtlasModels.cs` |
| 帧表读取 + 「必须是本轮产物」的新鲜度校验 + 按序号索引 | `Services/Atlas/AtlasSequenceManifestReader.cs` |
| 同步前给勾选动作打图集（只打勾选的、落 `tool/AtlasCache`、尺寸上限按帧数动态算） | `Services/Atlas/SequenceAtlasPackService.cs` |
| `--max-size` 参数 | `Services/Atlas/AtlasPackService.cs` |
| 计划契约：动作级带 `Atlas` 与 `SourceImages`，帧级带 `SourceImageIndex` | `Services/UnrealBridgeSequencePublishService.cs` |
| 同步流程接线：打包完成后再生成计划（进度条阶段 2/5） | `MainWindow.UnrealSync.Publish.cs` |

尺寸上限按「帧数 × 帧规格(928×640)」的面积开方、向上取到 2 的幂，2048 起、8192 封顶；
它只约束搜索范围，实际用多大由打包器挑、从 report 回读。

### 执行层（Python）

`Tools/UnrealBridge/sync_character_sequences.py`：

- 图集只 import 一次
- `_apply_atlas_sprite` 按矩形建或就地更新精灵，同时写入 `source_uv`、`source_dimension`、
  `b_trimmed_in_source_image`、`origin_in_source_image_before_trimming`、
  `source_image_dimension_before_trimming`、`b_rotated_in_source_image`
- Flipbook 按帧序引用这一批精灵（复用位置共用同一个指针）
- 清理基准改成「图集贴图 + 去重后的精灵 + Flipbook + 序列」

**没有改 ZDBridge 的 C++**。引擎的 `UPaperSprite` 那几个字段都是 `EditAnywhere`，
且 `PostEditChangeProperty` 结尾会走 `RefreshBakedData / RebuildCollisionData / RebuildRenderData`，
所以从 Python 写属性和 `.paper2dsprites` 导入器走的是同一条路。**这次不需要重新编译插件。**

### 差异层

- 序列动作不再走三方比较。载荷格式一换代旧基线两侧都对不上，
  三方比较会把全部动作判成冲突；现在直接比两侧当前载荷（`layout` + `slots` + `blanks` + 代号 + 帧率），
  不一致就是「需要重建」。
- 载荷里的 `layout`：图集布局写 `atlas:<图集名>`，仍是逐帧导入时写实际用到的贴图名集合。
- 计数改成资产口径：新增按素材去重并补一条图集贴图；删除以动作占用资产为准，不再按帧位摊开。
- 动作标题行补现状摘要：`工具箱 N 张素材 · M 个帧位 | Unreal 现有 K 个帧位`。
- 序列专属导出不再递归整个角色目录，只扫角色根（非递归）、`AnimSequences`、`Material`、角色 Item。
  之前虽然事后滤掉了 Sound/BUFF，但扫描本身还是把它们整个走了一遍。

### 验证到哪一步

- 主项目 `dotnet build -p:Platform=x64` 0 错误
- `python -m py_compile` 通过（`export_zd_assets.py`、`sync_character_sequences.py`）
- 未跑完整回归套件；未做实机同步

---

## 三、还没做完

### 1. 「Unreal 现有 0 个帧位」——已修，但过程值得记下来

现象：中栏的动作摘要里「Unreal 现有 N 个帧位」永远是 0。

**我先误判了两次**，记在这里免得重走：

- 先以为是缓存序列化丢了 `UnrealBridgeChange.UnrealItem`。
  直接打开 `%APPDATA%\CrossingVoidZDTool\UnrealSync\session-*-step5.json` 数，
  `"UnrealItem"` 出现 377 次且值是实对象 —— **序列化没问题**。
- 又以为 Unreal 帧没和工具箱帧配上对。探针数据显示
  `frames=288 withUnreal=154`，154 条是 `Unchanged`（配好对的），134 条是待新增 —— **配对也是好的**。

真实原因：**统计取错了数据源**。`FilterCachedPublishChanges` 会把 `Unchanged` 剔掉
（这是设计：缓存恢复出来的是待办清单），而配对成功的帧正是 `Unchanged`。
拿这份列表去数「Unreal 现有多少帧位」，永远是 0。

修法：帧位数改从**动作节点自己的载荷**里读 —— 动作节点是 `Updated`、不会被剔，
载荷里带着两侧各自的 `slots`。素材张数则按去重后的新增条目数。
读取辅助函数在 `SequenceFrameIdentity.TryReadActionPayload`。

**教训**：判断「数据丢没丢」要先去看磁盘上的产物，别从下游现象倒推。
这次两轮误判都是没先打开那个 6.7MB 的缓存文件看一眼。

### 2. 实机同步从没跑过（**最后验，也最关键**）

精灵的裁剪偏移是这次最脆弱的一环：写错不会报错，只会每帧被贴到画布左上角，整条动画抖。
源码语义已核对（`SetTrim` 令 `TopLeftUV = SourceUV - OriginInSourceImage`、尺寸取原图整幅），
但源码对不等于实机对。

建议先拿单个动作跑（Sk2：17 张素材、23 个帧位），在 Unreal 里确认三件事：

1. 贴图只有一张 `Misaka_Sk2`
2. 精灵 17 个
3. 播放时锚点不抖

### 3. 清理临时探针

`ViewModels/UnrealProjectSyncViewModel.cs` 里搜 `[DEBUG-seqfacts]`，定位完删掉。

---

## 四、几个容易踩的点（记下来免得重走）

- **图集只收 PNG**。素材池允许 jpg/webp/bmp，但图集侧是窄的 —— 混进有损格式会出现
  「本地看着没事、进引擎才发现边缘脏」。计划生成时遇到非 PNG 的帧会当场报错，不会静默当空白。
- **trim 必须开**。928×640 的帧不裁的话，2048 的图集一次只装得下 6 帧，Sk2 那 17 帧根本装不下。
  代价是精灵必须带裁剪偏移。
- **`_sequence.json` 是打包器写的，字段名是 camelCase**，和工具箱其余协议的 PascalCase 不同，
  所以它有独立的 `AtlasJsonContext`。混淆会导致读不到字段，而且是**静默丢弃**。
- **旧素材按约定由工具箱直接删**（用户已确认）。第一次真跑会删掉 `Material/<动作>/` 下
  那批旧的逐帧贴图和精灵。

---

## 五、2026-09-17 更新：实机同步已跑通，剩最后两处

Sk2 已实际同步成功。用探针在真实工程上核对过产物：

- `Misaka_Sk2` 图集贴图 1 张
- `Sk2_Flipbook` 23 个关键帧
- `Sk2_Frame00_Sprite` … `Sk2_Frame16_Sprite` 共 17 个精灵（17 张素材，正确）
- 旧的 23 张贴图清掉 22 张，剩 `Sk2_Frame00` 因仍被引用而保留
- Flipbook 的 frameRuns 合计 26、复用位置共用同一个精灵 —— **产物完全正确**

### 1. 复扫不展开停留格数（就是「Unreal 现有 23 个帧位」的来源）

计划里 Sk2 是 23 帧、`durationFrames` 合计 26（三帧各占两格）。
Flipbook 也确实是 26（探针实测 frameRuns 里有三个 2）。

但**导出/复扫只报 23**：`export_zd_assets.py` 的 `_ordered_flipbook_frame_assets`
没有按 `frame_run` 展开。于是检测拿工具箱的 26 去比 Unreal 的 23，
每个动作都被判成需要重建 —— 列表永远清不掉。

修法二选一，**推荐前者**：
- 修 `_ordered_flipbook_frame_assets`，按 `frame_run` 展开成 26；
- 或者把 `slots` 从比对载荷里去掉（治标，顺带丢掉「停留格数被改」的检测能力）。

### 2. 同步台页面偶发卡死（未查）

现象：点击右上角同步方向开关后、或在左栏选择角色后，**本页内所有点击失效**，
但页面还能滚动、也能切到别的 St 页面。看起来是这一页的某个异步操作没让
「操作进行中」的标志复位（`IsWorkflowOperationIdle` 之类），把整页的交互禁掉了。

没有查过，列在这里当第一条排查线索。

### 3. 探针脚本

三次属性名/数据形态的问题都是靠探针定的（精灵属性名、Flipbook 关键帧）。
脚本用完即删，模式可复用：写一个临时 py，用 `UnrealEditor-Cmd -run=pythonscript`
跑，把结果写 JSON 再读回来。比让用户跑一遍同步快得多。

---

## 六、2026-09-17 晚：两处「刷新了却看不出变化」的根因

Sk2 同步成功之后，界面依旧报「二技能 删除 24 项 / 新增 23 项」。查下来是**两个独立的
bug 叠在一起**，各自的证据都留在 `runtime.log` 与磁盘产物里。

### 1. 刚检测出来的结果被会话缓存盖回去（UI 侧）

日志（同一次刷新，相隔不到一秒）：

```
[DEBUG-seqfacts] step=5 frames=294 withUnreal=154 withToolbox=271   ← 刚检测完
[Refresh Selection Result] restored=0
[DEBUG-seqfacts] step=5 frames=163 withUnreal=23  withToolbox=140   ← 又被盖成旧的那份
```

路径是：`DetectUnrealPublishChangesAsync` 建完树 →`SetLoadedPublishStep(5)`
→`ReturnToWorkflowStep(5)`；而后者会走 `RestoreWorkflowStepCache(5)`，老老实实从
`tool/UnrealSync/sync-<key>-step5.json` 读回上一次写下的差异，**当场覆盖**刚建好的树。
用户看到的就是「点了重新加载，列表一点没变」。

第二份数字小的原因是另一半：缓存里存的是一份「待办清单」，
`FilterCachedPublishChanges` 按设计把 `Unchanged` 剔掉了。序列树的**动作节点自己就是
`Unchanged`**，被剔掉之后「Unreal 现有 N 个帧位」无从得知，帧也全部配不成对。

修法：

- `RestoreWorkflowStepCache` 增加「这棵树已经属于这一步」的短路条件
  （`_hasImportDetection && _loadedPublishStep == step && 树非空 && 角色一致`）。
  第三步与第五步共用同一个树的槽位，只有 `_loadedPublishStep` 分得开；
  换角色时 `SelectSource → ResetImportOperation` 会把它清零，冷启动照常从缓存恢复。
- 第五步的 `FilterCachedPublishChanges` **不再剔 `Unchanged`**（第三步照旧）。

### 2. 帧的身份在图集时代被比错了（协议侧）

导出清单里，一帧的 `objectPath` / `assetName` 是**贴图**。图集改造之后，
一个动作的 23 帧全都指向同一张 `Misaka_Sk2`，而 C# 侧算出来的「规范贴图名」是
`Sk2_Frame00..22` —— 永远对不上，于是每一帧都被判成「要改名」，
`ExpandSequenceChange` 把它摊成 `:delete` + `:add`，正好 23 对。

真正能区分帧的是 **Flipbook 关键帧挂的精灵**：复用位置共用同一个精灵，
所以 23 个帧位只对应 17 个精灵，而精灵名是按「素材目录里第几张图」生成的
（`SequenceActionCatalog.GetFrameSpriteName`，和打图集、生成同步计划同一套规则）。

修法（三段都要，缺一段就还是错）：

1. 导出侧 `_ordered_flipbook_frame_assets` 把关键帧上的精灵带出来
   （`spriteName` / `spriteObjectPath`）。精灵本来就已经读到了，以前是读完丢掉。
2. 工具箱侧 `UnrealBridgeToolboxSnapshotService.AddSequenceFrames` 按同一套规则
   算出每一帧**应当**落在哪个精灵上（枚举素材目录 → 文件序号 → 精灵名）。
3. `IsCanonicalSequenceFramePair` 在两侧都带精灵名时按精灵名比。
   **但只在 Unreal 已经换成图集布局的动作上生效**（动作载荷 `layout` 以 `atlas:` 开头）：
   还停在逐帧旧布局的动作，精灵名是历史导入留下的（按帧序号命名），
   拿它比工具箱按素材序号算出来的名字，会把本来没问题的帧判成要重做
   —— 这一条我在实机冒烟里踩到过，第一次跑反而把 Click 从「无差异」变成「4 删 4 增」。

### 3. 顺带修掉的重复计数

`ExpandSequenceLayoutRebuilds` 会给「需要重建」动作下的每一帧补一条 `:add`。
内容也变了的那一帧前面已经被摊成 `:delete`+`:add`，于是又补一层，出现 `:add:add` ——
同一张图被数两遍。现在跳过已经带 `:add`/`:delete` 后缀的条目。

### 4. 验证

- 回归套件：**292 通过 / 0 失败**（新增两条：`AtlasFramesPairBySpriteInsteadOfTexture`、
  `RefreshKeepsFreshSequenceTreeOverCache`）。
  另外 4 条是图集改造留下的**过期用例**，断言还停在线框时代的口径，一并按新契约改掉了：
  序列动作不再判冲突（没有独立编辑来源）、逐帧旧贴图现在算待删、生成计划必须自带图集。
- 实机冒烟（真实工程 `C:\CrossingVoid`，走真实导出 + 同上比对）：

  | 动作 | 新增 | 待删 | 无差异 |
  |---|---|---|---|
  | sk2（已同步） | 0 | 0（另有 1 条占用资产待删） | 23 |
  | 其余 18 个动作 | 各自要重建的数量 | 旧逐帧贴图 + 历史遗留 | 帧本身无差异 |

  整体从「共 477 / 新增 158 / 待删 169」变成「共 454 / 新增 135 / 待删 146」。

### 5. 还没做的

- **帧图片内容换过（同名同位置、只改了像素）检测不出来**。两侧的帧哈希天生不可比
  （工具箱哈希源 PNG + JSON 载荷，Unreal 哈希图集 PNG + 分隔符载荷），
  现在能看出来的是「帧用了哪张素材」。要真正 git 化，得在每次同步成功后把
  各素材的哈希记进状态文件，下次检测拿**当前素材哈希 vs 记下的哈希**比 ——
  路线已经清楚，等这一步先稳一版再做。
- 第五步从缓存恢复时保留了 `Unchanged`，缓存体积会大一些（Sk2 那份 step5 缓存 7.7MB）。
  如果之后变慢，优先看这里。
- 同步台偶发卡死（上文 3.2）仍未查。

---

## 七、2026-09-17 19:2x：复用帧让图集打包整体失败

用户点同步报：

```
动作「FlyStart」的素材目录里没有任何 PNG，不能生成图集。
```

而 St5 页面上 FlyStart 明明显示「1 张，尺寸合规」。

### 根因

帧**可以复用别的动作已经收进来的同一张图**：`SequenceFramePool.ImportSource` 遇到
角色自己的清单里已经引用过的帧，就直接指过去、不再复制一份，
清单里因此记着跨目录的相对路径。Misaka 的真实数据里有 23 帧是这样：

| 动作 | 自己目录里的 PNG | 帧实际引用 |
|---|---|---|
| FlyStart / FlyDown / Flying | 0 | 1（都在 Death） |
| Death | 3 | 8（另外 5 张在 Land） |
| Ko | 14 | 25（另外 11 张在 Victory） |
| Sub | 7 | 11（另外 4 张在 Sk1） |

而图集打包是「扫这个动作自己的 `Frames` 目录」：

- 自己目录一张都没有 → 报「素材目录里没有任何 PNG」（FlyStart 就是）；
- 自己目录有、但少 → 后面生成计划时报「帧素材不在本动作的图集里」。

两个出口同一个根因：**把「目录里有什么」当成了「这一轮要用什么」**。

### 修法

新增 `SequenceActionFolderLayout.ResolveSourceImages(frames)`：
「这个动作的帧实际引用到的图片，去重后按文件名升序」。四处统一改用它 ——

1. `SequenceAtlasPackService`（同步前打图集；空列表 = 整条空白帧 → 跳过，不再报错）
2. `AtlasPackService.PackAsync` 的入参从 `framesFolder` 改成显式的图片列表
3. `UnrealBridgeSequencePublishService.BuildSourceImages`（计划里的素材与精灵名）
4. `UnrealBridgeToolboxSnapshotService.ResolveExpectedSpriteNames`（检测时算期望精灵名）

排序仍按**文件名**，所以「自己目录里的图正好就是它引用的那些」的动作序号不变，
精灵名不变，已经同步过的 Sk2 **不需要重建**。实测确认：这些动作的 `Frames` 目录里
没有「存在但没被引用」的 PNG，所以这次改动对已同步动作是零扰动。

顺带：`BuildSequenceSyncPlan` 的跳过条件加上「一帧图都没有」——整条全空白帧的动作
不再把整批同步拦住（打包那边同样跳过它），错误文案也改成指向真正的原因。

### 验证

- 回归套件：**293 通过 / 0 失败**，新增「复用别的动作目录里的帧也能打包并同步」。
- 真实数据跑打包（走内置 Python + ue_atlas.py，Misaka 全部角色数据）：
  FlyStart / FlyDown / Flying / Death / Ko / Sub 六个动作全部打包成功，
  素材数 1 / 1 / 1 / 8 / 25 / 11，与上表一致，`tool/AtlasCache` 下产物齐全。
- 没有实机同步（只到「计划生成 + 图集产物」这一步）。

顺带确认：用户那次失败的同步是在**打包阶段**就抛了，计划没生成、Unreal 没被改，
工程里不会留下半成品。

---

## 八、2026-09-17 21:0x：`module 'unreal' has no attribute 'ObjectRedirector'`

18 个动作一起同步时，第 1 个（Sk1）成功，第 2 个（FlyStart）把整批打断：

```
[Sequence Execution] character=Misaka succeeded=False items=1
  error=module 'unreal' has no attribute 'ObjectRedirector'
```

### 根因

大小写对齐（`Ko`→`KO`、`Flystart`→`FlyStart`、`Standup`→`StandUP`）走的是
「改名 → 临时名 → 规范名」，两次改名各留一个重定向器，所以之后要
`_fixup_redirectors`。而它是这么判断的：

```python
if obj is not None and isinstance(obj, unreal.ObjectRedirector):
```

**UE 5.8 的 Python 里没有 `unreal.ObjectRedirector`。** 拿命令行走了一遍探针：

```json
{"has_ObjectRedirector": false,
 "has_fixup_referencers_tools": false,
 "has_fixup_referencers_class": false}
```

也就是说这一行必然抛 AttributeError，异常直接冒到批次顶层。
顺带发现：`AssetTools.fixup_referencers` 在这个版本同样不存在 ——
就算 isinstance 那行能过，原来的解引用也只会走进 except 分支、什么都不做。

### 修法

- 判重定向器改成**按类名**：`obj.get_class().get_name() == 'ObjectRedirector'`
  （`UObjectRedirector` 没暴露到 Python，类名是唯一稳定可查的）。
- 解引用改用 `unreal.EditorAssetLibrary.consolidate_assets(target, [redirector])` ——
  它做的是和编辑器「Fix Up Redirectors」同一件事：把引用方搬到目标资产上再删源包。
  `_align_asset_name_case` 现在把 `target_package` 一起传下来，不猜目标。
- **解不掉时宁可不删**：拿不到目标、或者 consolidate 抛异常，就记一条 warning 并把
  重定向器留在原地。删掉一个还有引用方的重定向器 = 引用永久悬空（这条路径打断过
  `Misaka_AnimBP` 的 Play Sequence 节点），比留个重定向器严重得多。
- 导出侧同时不再把重定向器当资产上报（`_is_redirector_asset`）：
  否则工具箱会把它列成「多出来的历史素材」待删，用户一勾就把引用删悬空了。

### 验证

- `python -m py_compile` 两个脚本通过；回归套件加了两组守卫断言：
  脚本里不得再出现 `isinstance(obj, unreal.ObjectRedirector)` 与 `tools.fixup_referencers`，
  导出脚本必须过滤重定向器。
- **没有实机重跑同步**（那会改用户的项目）。用户在界面上重试即为最终验证。
- 已经确认工程状态是干净的：Sk1 同步完整（图集 + 13 精灵 + Flipbook，旧贴图已清），
  其余动作没被改动 —— 崩在 `_align_*` 阶段时命令行走的是内存改名，重定向器包没落盘，
  `Material\FlyStart` 之类目录下没有 `*__ZDCaseMigration` 残留。

### 7. 顺带发现的两个问题（未做）

1. **一个动作炸掉会让整批后面的动作一起陪葬，而且日志里不出现是哪个动作。**
   `main()` 的 `for index, action in enumerate(actions)` 里直接 `_sync_action(action)`，
   异常冒到顶层，结果里只有 Sk1 那一条成功项 —— 这次的报错文案里根本看不出是 FlyStart。
   改法很小：循环里 try/except，把失败动作按 `stableKey=actionCode` 记成一条
   `succeeded=False` 的 item 再 continue（协议和 C# 侧都已经支持「部分同步」）。
2. **没有再系统性核对过 Python 绑定名。** 这次的教训是「`unreal.X` 想当然」会直接炸。
   已经做了一次静态清点（三个脚本里用到的 `unreal.*` 名字一共 22 个，
   只有 `ObjectRedirector` 是编的），但没有做成自动守卫 —— 值得写一条探针：
   导出 `dir(unreal)` 存成清单，和脚本里用到的名字对账。

---

## 九、2026-09-17 22:0x：把上面那两条补完

### 1. 一个动作失败不再拖垮整批（也不再"无名"）

`main()` 的循环原来是直接 `action_result = _sync_action(action)`，异常冒到顶层 →
结果里只剩前面成功的条目，日志里也没有动作名。现在循环里就地兜住：

```python
try:
    action_result = _sync_action(action)
except Exception as error:
    detail = '%s: %s' % (type(error).__name__, error)
    unreal.log_error('SequenceSync: action=%s failed: %s\n%s' % (code, detail, traceback.format_exc()))
    results.append({'stableId': code, 'itemKind': ITEM_KIND_ACTION, 'succeeded': False, ...})
    _write_progress(progress_path, index + 1, total, code, '失败：%s' % detail)
    continue
```

为什么可以直接继续：协议和 C# 侧本来就支持「部分同步」——
`failedActionCodes` 之类的语义早就有了（用例「基线只刷新执行成功的动作」），
缺的只是脚本这一层不把整批带走。于是一次同步里坏了一个动作，
剩下的动作照样做完，界面上写「部分同步」并把坏掉的那个点名出来。

### 2. Python 绑定名对账（探针 + 清单 + 守卫）

新增 `Tools/UnrealBridge/probe_unreal_python_bindings.py`：在**真引擎**里跑，
自己扫 `Tools/**/*.py`，用 `hasattr(unreal, 名字)` 逐个核对，输出
`{engineVersion, moduleNames(10622 个), scripts{名字, unknown}}`。

```powershell
UnrealEditor-Cmd.exe C:\CrossingVoid\CrossingVoid.uproject -run=pythonscript `
  -script=D:\UnrealMap\CrossingVoidZDTool\Tools\UnrealBridge\probe_unreal_python_bindings.py
```

第一次跑就抓到两类问题，都是「照着应该有的样子写」：

| 位置 | 编出来的名字 | 处理 |
|---|---|---|
| `export_zd_assets.py` / `sync_character_sequences.py` 的注释 | `ObjectRedirector` | 改成按类名判断（本页第八节） |
| 探针自己的兜底分支 | `ENGINE_MAJOR_VERSION` 等三个常量 | 删掉兜底，只用 `SystemLibrary.get_engine_version()` |

产物摘要落成 `Tools/UnrealBridge/unreal_python_bindings.json`（30 个名字，
带 `verifiedWith{engine, at, method}`），回归套件新增用例
「Python 脚本的 unreal 绑定名与引擎对过账」：扫 `Tools/**/*.py` 里所有
`unreal.<名字>`，凡是清单里没有的就报错，并提示重跑探针。
换引擎版本、或者要新用一个引擎 API 时，这道守卫会顶在最前面。

### 验证

- 探针实跑（`UnrealEditor-Cmd -run=pythonscript`，engine `5.8.2-0+UE5`）：
  `dir(unreal)` 10622 个名字，扫描 `Tools/**/*.py`，脚本用到的 30 个名字全部对得上。
- 回归套件：**295 通过 / 0 失败**（新增两条：「Python 脚本的 unreal 绑定名与引擎对过账」、
  「序列同步单个动作失败不再拖垮整批」）。
- 修好的脚本已同步进程序自己的 `bin\...\Tools\` 目录（哈希一致），
  运行中的实例下一次同步就是新逻辑。

---

## 十、2026-09-17 22:5x：整条复用帧的动作不该留一个空的 `Frames` 目录

用户看图时发现的：FlyStart 的 `ZDMaterial\FlyStart\Frames` 是个**空目录**，
而它的帧整条借用 Death 那张图 —— "省下来的东西"反而留下一个空壳，
看着像漏导了素材。

### 根因：空目录是好几处"顺手建一下"堆出来的

遍历动作时会经过 `LoadOrCreateManifest`，而它每次都
`Directory.CreateDirectory(GetFramesFolderPath(...))`；`SequenceManifestStore.Save`
和两条保存路径也各建一次。这些调用当初是为了"目录先备好，导入时直接写"，
但帧**可以整条复用别人的图**（`ImportSource` 发现已有引用就直接指过去、不复制），
于是对这类动作来说，那个目录永远等不到文件。

### 修法

- 新增 `SequenceActionFolderLayout.RemoveFramesFolderIfEmpty(character, action)`：
  只在目录**确实为空**时删；有文件、或者被占用就原样留着。
- 去掉预建：`SequenceManifestStore.Save`、`LoadOrCreateManifest`、`ImportFrames`、
  `SaveFramesAsync` 都不再建 `Frames`。真正要落文件的 `SequenceFramePool.ImportSource`
  照旧按需建。
- 收口清理：`LoadOrCreateManifest`（谁被读到谁就干净，升级后不用跑迁移脚本）与
  `SequenceFramePool.PruneUnreferenced`（删完帧图后顺手收掉）。

### 验证

- 回归套件 **296 通过 / 0 失败**，新增用例「帧全部复用时不留空的素材目录」：
  保存清单不建空目录、历史遗留的空目录被读到就收掉、自己目录里有图的动作不受影响。
- 真实工作区清了一遍：`D:\NewData\CrossingVoidZDProject` 下 39 个空 `Frames`
  目录全部删除（只删确认为空的，`Completed`/`Draft`/`Export` 三处都有）。
  现在 `ZDMaterial\FlyStart\` 里只剩 `sequence.json`。

---

## 十一、2026-09-17 23:5x：借用的素材不再重复打包（共享来源图集）

用户指出：帧复用别的动作的图时，那张图**已经在来源动作的图集里**了，
不该再打一份复制品 —— Misaka 实测这样的重复格有 23 个
（击飞/坠落/飞行 各 1 格全是复制品；死亡借落地 5 格、终结技借胜利 11 格、护援技借一技能 4 格）。

### 规则

- 图集只收**这个动作自己的**图；整条都借用别人的动作**没有**自己的图集。
- 每一格素材自带「用哪张图集 + 哪一格」：自己的图 → 本动作图集；借来的图 → 来源动作图集。
- 借用方仍然有自己的精灵和 Flipbook（击飞/坠落/飞行 的产物就是 sprite + flipbook）。
- 布局判定改成「这条序列用到哪些图集」的集合：自己的图集写 `atlas:<图集名>`，
  借了别人的写 `frames:<图集名按名字排序>`。两侧同源，Unreal 侧本来就是按每帧贴图名算的。
- **来源动作必须跟着同行**：借用方的矩形来自来源图集，来源不是当前那一版就可能对不上。
  勾选借用方时把来源动作一起纳入这一批（幂等重建），并按「先来源、后借用」排序。

### 影响面

| 层 | 改动 |
|---|---|
| 素材分类 | 新增 `SequenceActionFolderLayout.ResolveSourceImagePlan`：每张图带 `AtlasName` / `OwnerActionCode` / `IsOwn` |
| 快照 | 帧条目新增 `SourceAtlasName` / `SourceAtlasIsOwn`；动作载荷的 layout 按「用到的图集集合」算 |
| 打包 | `SequenceAtlasPackService` 只打自己的图；一张都没有就不打 |
| 计划 | `ResolveActionsWithSourceOwners` 连带来源动作；每张素材带 `AtlasName`/`AtlasImagePath`/`AtlasMaterialFolder`；动作级 `atlas` 允许为空 |
| Python | 按每格素材的图集导入（同一张只导一次）；动作级图集不再是必需 |
| 判定 | 规范资产名单里「图集」只在有自己图时才要；全借用动作不再补「图集贴图」行 |
| 顺带修的隐患 | 待删列表只收「动作占用的资产」行 —— 帧行的 Unreal 路径在图集时代指向共用图集，混进去会删掉规范图集 |

### 验证

- 回归套件 **298 通过 / 0 失败**。新增：借用的素材共享来源图集、借用方遗留的重复图集进差异待删；
  另加两条守卫：计划里每个字段（含素材项）都必须被桥接脚本读取、待删列表不收帧行。
- 真实工程比对（只读）：第五步从「无差异」变成
  **共 229 / 新增 53 / 更新 6 / 待删 56 / 无差异 114**，
  待删里正是那三张重复图集（`Misaka_FlyStart` / `Misaka_FlyDown` / `Misaka_Flying`）加上六个动作的重建行；
  更新的 6 个就是需要重建一次的动作（含连带同步的来源动作）。
- 真实数据跑了一遍「打包 + 生成计划」，结果：

  | 动作 | 自己的图集 | 用到的图集 | 借用 |
  |---|---|---|---|
  | 击飞 / 坠落 / 飞行 | 无 | `Misaka_Death` | 1 |
  | 死亡 | `Misaka_Death` | `Misaka_Death` + `Misaka_Land` | 5 |
  | 终结技 | `Misaka_KO` | `Misaka_KO` + `Misaka_Victory` | 11 |
  | 护援技 | `Misaka_Sub` | `Misaka_Sub` + `Misaka_Sk1` | 4 |

  打出来的图集只有 6 张（三个来源 + 死亡/终结技/护援技自己的），
  击飞/坠落/飞行 一张都没打。

---

## 十二、2026-09-18 上午：借来的精灵也一起借 + 素材内容级校验

### 1. 精灵也共享了（用户提的）

规则再往前走一步：**借来的图连精灵一起借**。那张图在来源动作里已经切好一只精灵，
借用方再建一只就是纯复制品。现在每个动作**只**拥有：

- 自己的图集（有自己素材时）
- 自己那几张素材各一只精灵
- 一个 Flipbook + 一个序列

实测（真实数据）：自建精灵 134 → **111**（省 23），
击飞/坠落/飞行 的产物变成**只剩一个 Flipbook**（示例精灵名 `Death_Frame0_Sprite` 之类）。
新增 `SequenceSourceImageResolver` 统一算「这一格用哪只精灵、要不要新建、在哪个目录」，
工具箱快照、同步计划、图集打包三处共用。

### 2. 素材内容级校验（用户要的）

同名图被换掉内容，以前看不出来：工具箱的帧哈希是「源 PNG + JSON 载荷」，
Unreal 是「图集 PNG + 分隔符载荷」，天生不可比。

做法：同步成功后把「这条序列用到的每张源图的内容哈希」记进
`<角色>/tool/UnrealSync/sequence-content.json`（自己的 + 借来的都记）。
下次检测时：

- 工具箱侧在动作载荷里写 `content:<当前摘要>`；
- Unreal 侧按 `ApplyRecordedContent` 补上**上次记下的**摘要；
- 两者不等 → 载荷哈希不同 → 这个动作判成需要重建（复用原来的差异树，不引入新概念）。

没有记录的动作不带这个字段 —— 否则「一边有、一边没有」会让每个动作都报变化。

**探针抓到一个坑**：注入记录值之后必须**重算 ContentHash**。差异比较比的是
ContentHash，改了载荷不重算，Unreal 侧还带着改之前那份哈希，于是两侧永远不等、
每个有记录的动作都被判成「素材变了」。这条是靠临时探针打出来的，不是想出来的。

### 3. 整条重建只占一行（用户要的）

一个动作重建时，逐帧的「第 N 帧（旧）/（新）」会摊出几十行（31 帧的终结技六十多行）。
现在帧行收成一行 **「重建整条（N 帧）」**，按文件删的（动作占用的资产）和「图集贴图」照旧一行一个；
动作行上的「删除 N / 新增 M」计数不变。用例：`整条重建只占一行`。

### 4. 顺手做的两件小事

- **旧图集缓存**：动作不再有自己的图集时，`tool/AtlasCache/<动作>/` 里上一轮的产物会删掉。
- **改完引用要存盘**（`_fixup_redirectors`）：`consolidate_assets` 只改内存里的引用方，
  commandlet 退出就丢了，而重定向器已经删掉 —— 引用永久悬空。现在改完会
  `save_directory(角色根, only_if_is_dirty=True)`。

### 5. 用户工程的 AnimBP 悬空引用（待修复）

上面那个存盘漏洞已经咬过一次：`Misaka_AnimBP.uasset` 里存着
`StandUP__ZDCaseMigration ×2`（大小写迁移的中间名），那个包早就没了。
证据：每次跑编辑器/命令都报
`Play Sequence references an unknown sequence` + `Missing allocated node ... PlaySequence_1`
→ commandlet 退出码一直是 1（日志里那句「进程退出码为 1，但同步结果标记为成功」就是它）。

修复脚本：`Tools/UnrealBridge/repair_dangling_sequence_reference.py` ——
把规范名再走一次「改名 → 临时名 → 规范名」让悬空引用重新能解析，
`consolidate_assets` 把引用方搬回规范名，**存盘**，再清掉重定向器。
默认修 `Misaka:StandUP`，可用 `ZD_REPAIR_SEQUENCES` 指定别的。

**跑它之前必须关掉编辑器**（它要写包），跑完再开。

**2026-09-18 11:48 实跑结果**：脚本在最后一步误报了一次失败（「改完名之后取不到资产或重定向器」）——
两次改名都成功了，但**重定向器已经被顺手清掉**，所以没有对象可 consolidate。
实际结果是对的：

- `AnimSequences` 里只剩 `StandUP.uasset`（规范名）
- `Misaka_AnimBP.uasset` 里的引用由 `StandUP__ZDCaseMigration ×2` 变成 `StandUP ×4` ✓
- 紧接着跑一次导出（冒烟里的那次 commandlet）**没有任何告警**、退出码 0 ——
  之前每次导出都带「进程退出码为 1，但同步结果标记为成功」，现在没有了 ✓

脚本已改成「没有重定向器可清」也算成功（存盘 + 收尾），不再误报。
注意：*改名本身就会改写引用方*，所以这条路即使不 consolidate 也是有效的。

### 7. 修好之后的差异现状（真实工程，只读比对）

```
第五步 序列（工具箱 173 / Unreal 361）：共 233，新增 30，更新 0，待删 60，冲突 0，无差异 143
  [DeleteCandidate] 死亡 · Death_Frame3..7_Sprite        ← 借用来的那 5 只精灵现在是多余的
  [DeleteCandidate] 坠落 · FlyDown_Frame0_Sprite         ← 整条借用之后这三只也多余
  [DeleteCandidate] 飞行 · Flying_Frame0_Sprite
  [DeleteCandidate] 击飞 · FlyStart_Frame0_Sprite
```

也就是说「精灵也能省」这件事，工具已经认出来了：勾选同步一次之后，
这些复制品会被清掉，借用来的帧改为直接引用来源动作那几只精灵。

### 8. 「借用的精灵不存在：…/Material/Sub/Sub_Frame0_Sprite」（2026-09-18 12:00）

8 个动作成功、Sub 失败。错误里那只精灵在 **Sub 自己的目录**里 —— 可 Sub 只借
Sk1 的 4 张图，自己那 7 只本该**新建**，怎么会被当成「借用」去找？

根因：**计划文件在磁盘上是 PascalCase**（`CreateSprite` / `SpriteMaterialFolder`），
而 Python 的 `_load` 里那份「字段归一化列表」没包含这两个新字段 →
`image.get('createSprite')` 永远拿到 None → 所有格子都走「借用」分支 →
连自己该建的那几只也去来源目录找 → 报出 Sub 目录里根本没有的精灵。

修法：把两个字段加进 `_load` 的归一化列表。核对方式：拿真实的计划文件跑一遍
归一化逻辑（不依赖引擎），确认 Sub 的 11 格里前 4 格 `createSprite=False`
（`Sk1_Frame00/02/10/11_Sprite`，目录是 Sk1），后 7 格 `createSprite=True`（Sub 自己建）。

**顺带把守卫用例收紧**：原来只检查「脚本某处出现过这个字段名」——
名字确实在正文里（`image.get('createSprite')`）出现，所以没拦住。
现在要求每个字段名必须出现在 `_load` 的归一化列表里（只截取 `_load` 函数体来查），
这类「静默读到默认值」的坑下次进不来。

### 6. 验证

- 回归套件 **300 通过 / 0 失败**（新增：借用的素材共享来源图集、借用方遗留的重复图集进差异待删、
  同名素材换内容能被检测出来、整条重建只占一行）。
- 真实数据探针：打包只出 6 张图集；自建精灵 / 借用精灵分别 3/5（死亡）、14/11（终结技）、7/4（护援技）、0/1（击飞/坠落/飞行）。
- 内容指纹探针：改动源图内容后，动作载荷的 `content` 从 `a4b0952a…` 变成 `1e55dd70…`，判定从 Unchanged 变 Updated。
