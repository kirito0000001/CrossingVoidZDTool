# 虚幻同步台

这里记录 CrossingVoidZDTool 与 CrossingVoid Unreal 项目之间同步领域的共同语言，避免后续把素材、数据和蓝图结构混为一谈。

## 同步范围

**发布阶段**：一次可单独检测、选择、执行和验证的同步工作单元。

**角色素材**：已完成角色在 Unreal 角色目录下的全部基础图片、序列帧图片、语音及相关素材；项目共享素材不属于角色素材。

**第一阶段素材发布**：角色素材发布中的首个阶段，当前覆盖角色 `AssetMaterial` 图片、`Sound` 语音和 `BUFF` 下的个人普通 Texture；`Material` 序列帧留给 ZD 动画轨道阶段，BUFF 蓝图及其内部数据留给 BUFF 阶段。

**素材规整工作区**：同步台中栏的独立工作模式，用于展示工具箱规范素材与 Unreal 现有素材的匹配建议；进入和返回不新增同步大分类，也不自动修改或删除 Unreal 资产。

**Item 数据**：角色 Item 资产中的角色基础信息、图片引用、技能信息和角色数值等可管理字段。

**ZD 基础序列**：角色目录 `Material` 下用于组成 PaperZD 序列帧的基础图片、Sprite 和序列资产。

**AnimMaps**：角色的动画地图资产，负责保存序列引用以及与角色动画播放相关的通知或映射关系。

**BUFF 蓝图**：角色 `BUFF` 目录中继承 DreamTask 体系的 BUFF 资产；同目录图标或普通贴图不属于 BUFF 蓝图本体。

**同步基线**：记录某个角色在指定 Unreal 项目中最近一次已验证状态的比较依据。

**在线执行**：目标 Unreal Editor 已打开时，通过 Python Remote Execution 在该 Editor 内执行扫描、写入、保存和复扫；只连接项目根目录与引擎根目录同时匹配的 Editor。

**离线执行**：目标 Unreal Editor 未打开时，通过 `UnrealEditor-Cmd` 执行同一份 Python 任务和同步计划。

**分步同步缓存**：同步进度按 `step1`、`step2`、`step3`、`step4` 分文件保存；进入某一步优先读取自己的快照，缺失或损坏时回退到其他步骤快照，最后兼容旧版项目单文件缓存。成功同步后第四步快照不保留已执行的第三步差异。

**检测结果摘要**：中栏只显示需要处理的差异；当列表为空时显示本次检查总数、无差异数量和各类改动数量。摘要计数随步骤缓存恢复，但切换方向、阶段或完成同步时清空。

**第四步基础配置**：第三步角色素材全部同步或确认无差异后进入。第四步独立扫描、缓存、勾选、应用和复扫角色入队语音、Item 白名单字段、受击 MetaSound 默认值和普通 SoundWave 并发；单项失败不得清空其他成功项。

第四步当前实现使用独立 `LightConfiguration` 请求/结果协议和步骤缓存，不复用第三步素材差异树。在线 Unreal 使用 Python Remote Execution，离线 Unreal 使用同一脚本的命令进程；应用前可按整体设置创建项目备份。

**基础配置白名单**：Item 只管理名称、介绍、关键词、道具图标、物品类型、数量、三组形态图片引用、基础战斗数值和被动介绍；不读取或修改 `CharShapeNow`、`CharShapeHas`、`SkillData`、`SkillNow`、`SkillHave`、`SkillLevel`、`Synchronize`、`Anti` 及蓝图图表。Hurt SoundWave 本身不设置并发，受击并发只写在角色 OnDM MetaSound；非 Hurt 角色语音只保留当前角色 `Con_Talk`。

**语音规范名称**：工具箱语音文件名中的连字符 `-` 是合法且需要保留的 Unreal 资产名字符；路径比较和发布目标生成不能把它改写为下划线。

**素材内容指纹**：图片、语音等文件素材以工具箱源文件的原始 SHA-256 作为内容指纹；Unreal 导出预览和语义 Payload 不参与源文件指纹比较。旧状态缺少当前哈希协议标识时不参与比较；没有有效历史基线时，只把当前步骤中两侧可唯一配对的已有项迁移为新基线，避免把已同步素材误判为冲突。

**基线增量提交**：同步成功后只更新本次执行并通过复扫验证的稳定 ID；未执行项保留已有基线，删除项移除对应基线，当前步骤不得提前写入后续步骤的成功状态。

首次发布检测不要求已有同步基线；先确认角色蓝图和目标目录存在，再生成当前差异。同步基线用于历史变更判断和已存在路径恢复。

**合并**：在不覆盖另一侧未管理内容的前提下，把两侧可识别字段组合为一个结果；它不同于整项覆盖。

## 原则

- 已完成角色的角色素材可以作为一个独立发布阶段整体处理。
- 第一阶段只处理 `BUFF` 文件夹中明确识别为普通 Texture 的个人图标，不覆盖 BUFF 蓝图，避免破坏 BUFF 资产身份。
- Item 数据只更新工具箱明确管理的字段，保留其他 Unreal 字段。
- ZD 基础序列与 AnimMaps 分开处理；动画通知和映射关系不能因序列帧更新而被覆盖。
- BUFF 的内部委托、函数和触发逻辑不属于工具箱第一版同步范围。
- 发生冲突时可以选择保留 Unreal、使用工具箱、跳过，或对明确可合并字段执行合并。
- 工具箱角色代号必须与 Unreal 角色目录名完全一致；拼写差异直接报错，不做兼容映射。
- 在线与离线执行复用同一份任务脚本、计划、进度和结果协议，发布阶段不感知执行通道。
- 在线执行只保存本次操作影响到的资产和引用包，不得保存用户在 Editor 中打开的其他脏包。
