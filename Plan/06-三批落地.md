# 三批落地

> 范围按 `05-待决问题.md` C1 的答复：**做到「乙」= P0 + P1**。
> C2/C3/C4 已批准，放进第三批。文档随 git 保留，以后有机会继续 P2 及以后。

---

## 已确定的决策（来自你的回复）

| 项 | 决定 |
|---|---|
| 音效分类 | `Kind=SoundEffect`、`DisplayName=音效`、`FolderName=SoundEffect`、`FileSuffix=SE`、**可选** |
| 音效并发 | **不走** `Con_Talk` 并发（音效可叠加） |
| `Other` 并发 | **保留在并发里**（语音不能重音，无论是否已分配） |
| Voices 待删除 | **改成可执行**（素材两侧要对应） |
| `PublishTrimmed` | 先不动 |
| 旧文档 | 基本用不上，**重写 README**，去掉写死的机器路径和外发指令 |
| Unreal 工程 | 两边一样，`I:\UnrealProject_Moon\SRC_REPO\CrossingVoid` 可以用来实测 |

**B1 的重要更正**：不是「失败语音被误分到 Other」，而是**「设置好的 Defeat 语音没有被同步上去」**——Other 里那两条是历史遗留的未使用语音。这是发布方向的 bug，与导入方向的分类无关，已单独派查。

---

## 第一批：正确性修复

目标：**直接减少 bug，你能立刻感知**。全部是已确认根因、改动局部的修复。

| # | 缺陷 | 位置 | 说明 |
|---|---|---|---|
| 1 | 蓝图保存失败被吞，报「错误 0」 | `apply_blueprint_setup.py:608` | 上个提交的漏网，同一形状只修了一半 |
| 2 | 关窗死锁 | `MainWindow.CharacterInfo.cs:96` + `CharacterDeskViewModel.cs:300` | 改过角色信息后关窗必卡死 |
| 3 | 「原子写」实为删除再移动 | `CharacterWorkspaceService.cs:779` + `:644` + `:664` | 崩溃即角色数据整份消失 |
| 4 | 第 3/5 步勾选不刷新计数 | `UnrealProjectSyncViewModel.cs:2099` | 对照组证明是遗漏 |
| 5 | 其他 6 处漏通知 | 见 `03-缺陷清单.md` M5/M6 | 每条配一个 PropertyChanged 用例 |
| 6 | 新增「音效」分类 | Models/Services/python/tests | 按上表决策 |
| 7 | 语音分类三处修复 | `UnrealBridgeVoiceClassification.cs` 等 | 序列反推回退、Defeat token、`Ondm`→`OnDamage` |
| 8 | Voices 待删除可执行 | `UnrealBridgePublishSupportPolicy.cs:19` | |
| 9 | 删 5 个零调用 public 方法 | `UnrealProjectSyncService.cs` | 编译器兜底，零风险 |
| 10 | 补 2 个零覆盖纯函数服务的测试 | `UnrealAssetNormalizationService` / `UnrealBridgeVoicePathPolicy` | 为后续重构建安全网 |
| 11 | Defeat 语音同步不上去 | 调查中 | B1 |
| 12 | 完成角色观看模式看不了序列 | 调查中 | 你补充的 |

**并行策略**：第 1 项是纯 Python，与 C# 完全不相交，可以并行；其余 C# 修复全都汇聚到 `Tests/Program.cs` 这一个 1 万行文件上，并行改会互相冲突，所以按顺序做（构建 11 秒、测试很快，顺序做并不慢）。11/12 两项由子分析并行调查中。

---

## 第二批：横切设施（P1）

目标：**让「显示成功但实际没做成」这一族 bug 从结构上不再产生**。

| # | 内容 | 消灭什么 |
|---|---|---|
| 1 | `Services/Infrastructure/IToolboxLog` | Services 层 16800 行 **0 处日志出口**、约 90 处静默 `catch` |
| 2 | `Services/Infrastructure/AtomicFileWriter` | 散落 12 个文件的 `.tmp+Move`、6 份 `TryDelete`；顺带补上**完全没有的重试** |
| 3 | `Services/UnrealBridge/UnrealProcessRunner` | 4 份 `KillProcessTree`、4 份进度轮询（间隔还各不相同） |
| 4 | 取消时真正杀掉 Unreal 进程 | 3 处 `Task.Delay` 里取消 → 进程变孤儿占着工程锁 |
| 5 | 结果文件新鲜度校验 | 2 处会读到**上一轮**的结果 → 第五步写错基线 |
| 6 | 子进程输出编码固定 UTF-8 | 5 处中文诊断信息乱码 |
| 7 | 常量外提（`UnrealContentPaths` / `CharacterFolderLayout`） | 底层服务反向引用 God Class 的 14 处 |

---

## 第三批：机制与防漂移

目标：**让第一、二批的成果不再退化**。

| # | 内容 |
|---|---|
| 1 | 引入 `CommunityToolkit.Mvvm`，把 11 处漏通知改成 `[NotifyPropertyChangedFor]` 声明 |
| 2 | 把 73 处「断言源码文本」逐步换成行为断言 |
| 3 | 加护栏用例：文件体量只降不升、禁止静默 `catch`、语音分类 C#↔Python 漂移校验 |
| 4 | 重写 `README.md`；清理旧文档里写死的机器路径和外发指令 |
| 5 | 剩余中等缺陷（区域文化解析、`Ordinal` 大小写、导出脚本两处、伪条目计数等） |
| 6 | 12 个遮罩层抽成 `Controls/*.xaml`（C3 已批准，视时间） |

---

## 两个贯穿始终的模式

**路径大小写**（4 个实例）：第六步已修、第五步清理误删刚生成的资产、资产规整判定、导出脚本两处。
→ 第二批加一个「资产路径一律大小写不敏感比较」的公共函数，把 4 处统一。

**显示成功但实际没做成**（6 个实例）：蓝图保存、settings 损坏、陈旧结果文件、伪条目计数、顶层 succeeded 恒 True、第三步不读执行结果。
→ 第二批的 `IToolboxLog` + 「失败必须上报」纪律。
