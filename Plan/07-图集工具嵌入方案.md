# 07 · 图集工具嵌入方案

> 状态：**方案已收敛（5 件已定，无待拍板项）**（本文件只定方案，不含已落地的代码改动）
> 前置事实：`D:\图集\ue_atlas_tool` 已完成装箱算法优化（货架式 + 旋转姿态搜索，密度 94.1%），
> 清单接口 `--manifest` 就绪。本方案讨论怎么把它接进 ZD 工具箱。
>
> **2026-09-15 第三轮更新**：按晓桀的答复收口 ——
> ① 用实机资产（`C:\CrossingVoid` 的 Misaka）验证了 Flipbook 与复用帧的机制；
> ② 两套实现全搬（一个程序不能分两半）；
> ③ 空白帧按实机结论定为「不出图」；
> ④ **Python 内置**（「内置一个吧，也不大」）；
> ⑤ 「有些序列甚至没有帧」**不处理** —— 那是插件侧的事，图集不该为它加特例。

---

## 一、结论先说

**做成「进程 + 文件」两段式，不做 C# 重写、不 Embed Python。**
**两套实现（Python + Node）全部搬进工具箱，`D:\图集\` 之后删掉 —— 一个程序不拆两半。**

工具箱只负责三件事：

1. **把清单写出去**（它已经有这个数据了，就是 `sequence.json`）
2. **起一个进程**（复用 `UnrealProcessRunner`）
3. **把产出的尺寸读回来**（`--report`）

工具箱**永远不知道**装箱、旋转、密度这些概念 —— 那是图集工具内部的事。
这条边界划清楚，两边就能各自演进。

---

## 二、为什么不是另外两条路

| 方案 | 否决理由 |
|---|---|
| **C# 重写装箱** | 等于长期维护两份实现。图集工具那 55+33 项自检、拆 TexturePacker DLL 得来的算法结论、旋转取图约定，全部要重新踩一遍。而且它那个 `grid` 模式（给 Niagara SubUV）是独有的，重写还得再实现一遍。 |
| **Embed Python（Python.NET）** | 把「进程边界」换成「运行时边界」，耦合更紧不是更松：Python 版本、Pillow 依赖、异常跨语言传递，全都变成工具箱的启动负担。而且失败时会**拖垮主进程**，而进程方式最多是子进程挂掉。 |
| **进程 + 文件（采纳）** | 已有 `UnrealProcessRunner` 解决退出码不可靠 / 杀进程树 / 进度轮询；已有 `AtomicFileWriter`；Python 侧自检不需要 Unreal 就能跑（和现有 `check_export_asset_paths.py` 一个套路）。 |

---

## 三、落点：两个场景、两种位置

你（晓桀）的原话：「St5 要求的单独导出，那就是在导出区域；同步序列那里的话，其实是一次性的，可以放进可删除缓存里面。」

按这个分成两条链路：

### 场景 A · St5 手动导出（用户要的产物）

```
<工作区>/Export/<角色代号>/Atlas/<动作代号>/
    <角色>_<动作>.png
    <角色>_<动作>.paper2dsprites
    <角色>_<动作>_sequence.json
```

- 走 `CharacterFolderLayout.Export`
- **是交付物**，用户会拿走 → 不自动清理
- 界面上要能看到尺寸、能打开目录

### 场景 B · 序列同步时顺带打（一次性）

```
<工作区>/<Draft|Completed>/<角色代号>/tool/AtlasCache/<动作代号>/
```

- 挂在 `tool/` 之下（工具箱自己的数据区，和 `CharacterBackups` 平级）
- **用完即弃** → 每轮同步开始前清一次，或按「比这轮开始时刻旧」清理
- 需要新增一个目录常量：`CharacterFolderLayout.AtlasCache = "AtlasCache"`

> ⚠️ 放在 `tool/` 下是**有意的**：`CharacterWorkspaceService.EnsureCharacterLayout` 与
> 导出/备份逻辑都会碰角色目录，缓存必须待在一个「不会被当成素材交付出去」的地方。
> `tool/` 正好是那个语义。

---

## 四、接口契约（工具箱 ↔ 图集工具）

### 4.1 出去：清单文件

工具箱**已经有一份能直接用的数据** —— `SequenceFrameManifest`。映射关系天然成立：

| 图集清单字段 | 来源 |
|---|---|
| `frames[].file` | `SequenceFrameManifestEntry.RelativePath`（相对动作目录解析成绝对路径） |
| `frames[].name` | 与 Unreal 侧**同一套命名函数**生成（见 §4.3） |
| `frames[].index` | **该帧在 `Frames` 数组里的原始下标 +1**，空白帧照常占位 |
| `atlas` | `<角色代号>_<动作代号>` |

### 4.2 「一帧一行、序号不压缩」是硬要求（**已用真实工程验证，不是推测**）

这一点我原先写反了理由，晓桀让我重查 —— 查了脚本和**实机资产**，结论如下。

**Flipbook 侧代码（`Tools/UnrealBridge/sync_character_sequences.py:559-592`）**

```python
for ordinal, frame in enumerate(frames):
    if frame.get('isBlank'):
        imported.append(None)          # 空白帧不建贴图，但占数组一位
    ...
sprites = [None] * len(frames)          # 按帧数预分配
frame_runs = [max(1, int(frame.get('durationFrames', 1) or 1)) for frame in frames]
```

**C++ 侧（`ZDBridge/Source/ZDBridge/Private/ZDBridgeLibrary.cpp:97-102`）**

```cpp
for (int32 Index = 0; Index < Sprites.Num(); ++Index)
{
    FPaperFlipbookKeyFrame& Key = KeyFrames.AddDefaulted_GetRef();
    Key.Sprite   = Sprites[Index];
    Key.FrameRun = FrameRuns.IsValidIndex(Index) ? FMath::Max(1, FrameRuns[Index]) : 1;
}
```

→ **`key_frames` 的数量 == 序列帧清单的长度**，`FrameRun`（驻留格数）**完全不改列表长度**。

#### 实机验证（`C:\CrossingVoid`，Misaka）

拿工具箱自己的工作区 `D:\NewData\CrossingVoidZDProject\Draft\Misaka\ZDMaterial\*\sequence.json`
和 Unreal 侧的产物逐条对：

| 动作 | 清单帧数 | UE sprite 数 | 结论 |
|---|---|---|---|
| Click | 4 | 4 | ✅ 相等 |
| Death | 8 | 8 | ✅ |
| Defeat | 1 | 1 | ✅ |
| Flying / FlyDown / FlyStart | 1 | 1 | ✅ |
| **Ko** | **31** | **31** | ✅ 其中 6 格复用 |
| **Sk2** | **23** | **23** | ✅ 其中 6 格复用（Frame17..22 = Frame05..10） |
| **Victory** | **19** | **19** | ✅ 其中 8 格复用 |
| Idle / Sk1 / Sub / … | 8 / 13 / 11 | 同 | ✅ |

**复用帧的实机证据**（读 `.uasset` 里的源贴图名）：

```
KO_Frame00.uasset        -> Misaka-Victory-404eff5af365cdcf.png
Victory_Frame00.uasset   -> Misaka-Victory-404eff5af365cdcf.png   ← 同一个源文件
Sk2_Frame05.uasset       -> Misaka-Sk2-5ae23a0f49d9f90e.png
Sk2_Frame17.uasset       -> Misaka-Sk2-5ae23a0f49d9f90e.png       ← 同一个源文件
```

**关键结论**：跨动作复用**不共享资产**。KO 的 Frame00 是 `KO_Frame00_Sprite`，
Victory 的 Frame00 是 `Victory_Frame00_Sprite` —— 两份独立 sprite，指向同一张源 PNG。
同理 Sk2 内部复用也是 `Sk2_Frame05_Sprite` 和 `Sk2_Frame17_Sprite` 两份独立资产。

> 🔴 所以「有些序列甚至没有帧，因为用的都是其他帧用过的」这句话，**按现在的实机产物是不成立的** ——
> 每个动作都把自己的帧位填满（要么建贴图，要么留 `None`），一条序列不会因为「图是借来的」就变短。
> 如果晓桀见到的确实是「零帧序列」，那是另一件事（可能是别的角色/别的版本），要单独看。

#### 由此推出的三条硬规则

1. **图集清单必须逐帧 1:1**（`frames[]` 的条数 = 序列帧清单长度，空白帧除外）。
2. **复用帧必须出多条记录**，每条一个独立的 sprite 名（`Frame05_Sprite` 和 `Frame17_Sprite`）。
   —— **我上一版说「复用帧只出一次」是错的**，那样会让 Flipbook 少帧。
3. **空白帧不能出成透明图**。它有 `DurationFrames` 语义但**没有像素**；图集里跳过它。
   而**序号绝不能因为跳过它而压缩** —— 序号是资产名的组成部分（见 §4.3）。


### 4.3 精灵名必须复用 Unreal 侧同一套生成规则

**这是全套方案里最容易出错的一处。** 图集工具产出的 `_sequence.json` 是给
Unreal 侧建 Flipbook 用的，`name` 字段就是将来的 **Sprite 资产名**。
它必须和 `SequenceActionCatalog` 算出来的一模一样，否则同步时会认为「这是个新增 sprite」，
插进去一份重复的。

现成函数（**直接调，不要重写**）：

```csharp
SequenceActionCatalog.GetFrameTextureName(definition, formIndex, ordinal, totalFrameCount)
// => "<VariantCode>_Frame<补零序号>"    例：Misaka_Sk2_Frame00
SequenceActionCatalog.GetFrameSpriteName(definition, formIndex, ordinal, totalFrameCount)
// => 上面那个 + "_Sprite"               例：Misaka_Sk2_Frame00_Sprite
```

规则细节：**序号从 0 开始**，位宽 = `MaterialSequenceNaming.GetWidth(totalFrameCount)`
即 `totalFrameCount.ToString().Length`（十帧就是两位 `00..16`）。

> 🔴 **注意**：`ordinal` 是**原始数组下标**（`UnrealBridgeSequencePublishService.cs:189-206`
> 特意写了注释「空白帧同样占一个位置，这样资产编号和 Flipbook 的关键帧下标始终一一对应」）。
> 所以算名字时 `ordinal` **不能**用「过滤掉空白帧之后重新数的号」。

### 4.4 回来：`--report` 的结果

按你的要求「**只给尺寸就行了**」：

```json
{
  "ok": true,
  "atlas": "Misaka_Sk2",
  "size": { "w": 1115, "h": 1019 },
  "image": "Misaka_Sk2.png",
  "frameCount": 17
}
```

工具箱只需要 `size.w` / `size.h`（显示 + 记进清单）。其余字段留着不解析也没关系。

> 📌 **前提**：`--report` 是**唯一的返回值**。退出码不可靠这条在 Unreal 侧已经吃过亏
> （`CONTEXT.md` 里写着），Python 侧同理。判定成败一律**看 report 文件是否新鲜且 `ok:true`**，
> 不看退出码。

---

## 五、要动的地方

### 5.1 图集工具**整体**搬进仓库

`D:\图集\ue_atlas_tool` → `<仓库>/Tools/Atlas/`

```
Tools/Atlas/
    ue_atlas.py            # 主力实现（纯 Python + Pillow）
    pack_ue.js             # Node 侧入口
    shelf_packer.js        # Node 侧货架装箱器
    MANIFEST.md            # 契约文档（对外的）
    INTEGRATION.md
    README.md
    examples/
        unreal_flipbook_from_sequence.py
    tests/check_atlas.py
    tests/check_pack_ue.js
    python/                # 内置解释器 + Pillow（见 §5.4），随仓库走
        python.exe
        Lib/site-packages/PIL/…
```

**两条实现都搬。** 你说「把功能全部搬进去之后我就打算删除源文件了」，那就按
**源码唯一副本在工具箱仓库**来：`D:\图集\ue_atlas_tool` 搬完即删，不再保留第二份。

配套规则：

- `node_modules/` **不搬**（体积大、可重装）。搬完在 `Tools/Atlas/` 下 `npm install`
  一次，并把 `node_modules/` 加进 `.gitignore`。
- `demo/` 里的帧素材是测试用的，**搬**（自检要用），但 `demo/out*` 这些产物目录删掉。
- **`Tools/Atlas/` 下所有东西都属于「外部工具」性质**，不进主工程编译
  （`.csproj` 不要 glob 到这个目录）。
- 搬完**两条自检都要能在仓库里跑通**：
  ```
  python Tools/Atlas/tests/check_atlas.py        # 55 项
  node   Tools/Atlas/tests/check_pack_ue.js      # 33 项
  ```

### 5.2 新增服务

`Services/Atlas/AtlasManifestWriter.cs`

- 从 `SequenceFrameManifest` + `SequenceFrameAction` + `SequenceActionDefinition` 生成图集清单
- 精灵名调用 `SequenceActionCatalog.GetFrameSpriteName(...)`（§4.3），不自己拼字符串
- 空白帧：**占位但不进 `frames[]`**，序号按原始下标算
- 复用帧：同一 `RelativePath` 出现多次时，**每个位置都要出**（因为 sprite 名不同：
  `Frame05_Sprite` 和 `Frame17_Sprite` 是两张资产）。**实机已确认**：Sk2 的 Frame17..22
  与 Frame05..10 指向同一批源 PNG，但 Unreal 侧建的是 23 份独立 sprite。
  清单必须跟着出 23 条，缺一条 Flipbook 就少一帧。真正要防的是**同名**，不是同图。
- 落盘走 `AtomicFileWriter`
- 纯函数式，好测：`BuildManifest(character, action, manifest) -> string(json)`

`Services/Atlas/AtlasPackService.cs`

- `Task<AtlasPackResult> PackAsync(character, action, manifest, AtlasDestination dest, IProgress<...>, CancellationToken)`
- 内部：写清单 → 起进程 → 等 → 读 report → 校验新鲜度
- 起进程复用 `UnrealProcessRunner.RunAsync`，`verdict` 判定用「report 文件新鲜 + ok」

`Models/AtlasModels.cs`

- `AtlasPackResult(string AtlasName, int Width, int Height, string ImagePath, int FrameCount)`
- `AtlasDestination`（枚举：`Export` / `Cache`）

### 5.3 目录常量

`Services/Infrastructure/CharacterFolderLayout.cs` 加：

```csharp
/// <summary>图集临时缓存，放 tool/ 下，一次性、可删。</summary>
public const string AtlasCache = "AtlasCache";
```

### 5.4 Python：**内置一个**（已拍板，且已实测跑通）

晓桀原话：「**内置一个吧，也不大。**」—— 实测下来确实不大，见下面的数字。

#### 实测体积（2026-09-15 真下的包，不是估的）

| | 体积 |
|---|---|
| `python-3.13.14-embed-amd64.zip`（下载包） | 10.5 MB |
| 解压后 | 21 MB |
| **加 Pillow 11.3.0 后（实际要进仓库的）** | **35.2 MB** |
| **压缩后（git 里增加的量）** | **17.7 MB** |

> 参照：本机完整版 Python 是 **165MB**（其中 `Doc/` 62MB、`tcl/` 7MB 都白搭），
> embeddable 省掉的就是这些。**17.7MB 进 git 完全可接受**，「也不大」这个判断是对的。

#### ⚠️ 实测踩到的坑：`_pth` 必须显式加一行，否则 PIL 导不进来

embeddable 包靠 `python313._pth` 做**路径隔离**（不读注册表、不读环境变量），
它默认只有两行：

```
python313.zip
.
```

**这种状态下 `import PIL` 直接 `ModuleNotFoundError`** —— 我把 `PIL/` 放进
`Lib/site-packages/` 之后仍然找不到，因为 `_pth` 模式**不会自动加 site-packages**。

修法是把 `_pth` 改成三行：

```
python313.zip
.
Lib/site-packages
```

改完 `import PIL` → `11.3.0`，实测通过。
**这一条一定要写死进部署步骤**，否则「解压完看起来对、一跑就找不到模块」。

#### 为什么 Pillow 要「解进去」而不是 `pip install`

embeddable 包**不含 pip**（无 `ensurepip`，也不认注册表）。
常见做法是「解压 → `get-pip.py` → pip install」，但那需要**联网**，
还会在用户机器上留 `Scripts/` 和一堆中间产物。

**改用「解压即用」**：Pillow 的 wheel 里就是一个顶层 `PIL/` 目录，
直接整目录拷到 `Lib/site-packages/PIL/` 即可。**不联网、不调 pip、不留中间文件。**

Pillow 的 C 扩展（`_imaging.cp313-win_amd64.pyd` 等 8 个 `.pyd`）就在 `PIL/` 目录里，
embeddable 的 loader 能直接吃 —— **实测通过**，见下。

#### 实测验证（都跑过了）

```
① Tools/Atlas/python/python.exe -c "import PIL; print(PIL.__version__)"
   -> 11.3.0                                        ✅

② 完整功能探针（读写/像素/旋转）
   写图 OK / 读图 OK (64,48) RGBA / 像素 OK (255,0,0,255) / Transpose OK   ✅

③ python.exe Tools/Atlas/tests/check_atlas.py
   -> 全部通过（55 项）                              ✅

④ 端到端真打一张图集
   python.exe ue_atlas.py --manifest … --report …
   -> [pack] DemoAtlas.png  359x391  sprites=12  密度 86.6%  (shelf:h)
   -> report: {"ok": true, "size": {"w":359,"h":391}, "frameCount": 12}   ✅
```

**结论：内置方案不是纸上推演，是实测可行的。** 55 项自检 + 端到端出图都过了。

#### `Tools/Atlas/python/` 进不进 git

**进 git。** 理由就是你那句「一个程序怎么能分开两个下载」——
Python 也是「程序的一部分」，跟着仓库走才不会丢。**17.7MB**，可接受。

> 备选（**不推荐**）：不进 git、由 `Pakout.ps1` 去取。仓库干净，但打包脚本一旦漏了，
> 打出来的包就是坏的、而且本地测不出来。和你的诉求也相反。

#### 探测链保留（作为覆盖手段）

内置是**默认**，但仍按顺序找，允许覆盖：

1. **整体设置里配置的路径**（给「我想用自己的 Python」留的口子）
2. **`Tools/Atlas/python/python.exe`** ← 内置的，正常情况下走这条
3. `py -3` / `python` / `python3`（兜底：内置目录被误删时还能活）

每一档都做 `import PIL` 探针；**全失败就报错，不静默回退**（`README` 的纪律）。
探测结果进程内缓存，设置改了立刻失效重探。

> ⚠️ 内置的 Python 是**相对工具箱可执行文件**的路径，不是相对源码目录。
> `Pakout.ps1` 里要确认 `Tools/Atlas/python/` 会跟着走（和 `Tools/UnrealBridge/*.py`
> 是同一类东西）。这一步不做，本地跑得好好的，打出来的包一导出就报「找不到 Python」。

### 5.5 Node 侧：**一起搬，但工具箱不调它**

晓桀原话：「**全搬，一个程序怎么能分开两个下载，丢个文件直接用不了了。**」

这句话是对的，而且否掉了我的推荐 —— 按「一个程序」来：

- **`pack_ue.js` + `shelf_packer.js` + `tests/check_pack_ue.js` 全部搬进 `Tools/Atlas/`**，
  和 Python 侧并列。`D:\图集\` 搬完即删（§7.1）。
- 工具箱的主流程**只调 Python**，不检测 Node 是否存在、不因缺 Node 报错。
- Node 那条路的价值是：**自检**（`node Tools/Atlas/tests/check_pack_ue.js`）、
  以及将来想用时它就在手边，不用再上哪儿找一份。

**要不要把 Node 也变成「内置」的？** —— 见 §5.4 末尾的讨论，我倾向**不内置**，
理由是它现在不参与主流程，为一个跑不到的运行时装 50MB 不划算。
真到要用的时候（比如某个导出格式 Python 侧没有），再按同样的「探测 + 设置项兜底」接一遍。

### 5.6 界面接线

两处按钮，都走同一个服务：

| 位置 | 文案 | 落点 |
|---|---|---|
| St5 序列帧页 | 「导出图集」 | `Export/<角色>/Atlas/<动作>/` |
| 序列同步台 | （不出现按钮，同步流程内自动） | `tool/AtlasCache/<动作>/` |

---

## 六、怎么验

按仓库「断言行为、不断言源码文本」的纪律：

1. **Python 自检照跑**：`Tools/Atlas/python/python.exe Tools/Atlas/tests/check_atlas.py`（55 项）
   —— **用内置解释器跑**，这样顺带验证了「内置的 Python 能跑通」。
   如果这条挂了，说明内置包装歪了（比如 Pillow 的 C 扩展位数不对）。
2. **内置解释器自检**（新增，切到 §5.4 的要害）：
   `Tools/Atlas/python/python.exe -c "import PIL; from PIL import Image; print(PIL.__version__)"`
   —— 必须成功。**这是唯一能在打包前发现「PIL 装歪了」的地方**，
   否则要到用户点导出时才炸。
3. **Node 自检照跑**：`node Tools/Atlas/tests/check_pack_ue.js`（33 项）
4. **新增 C# 用例**：清单生成
   - 空白帧：不进 `frames[]`，**但后面的帧号不塌陷**（有空白帧在中间时，
     第 5 帧仍叫 `Frame04`，不变成 `Frame03`）
   - 复用帧：同一 `RelativePath` 出现两次 → 出**两条**记录、两个**不同**的 sprite 名
     （拿 Sk2 的真实清单做输入最直接：23 帧 / 17 张图 → 必须出 23 条）
   - 精灵名与 `SequenceActionCatalog.GetFrameSpriteName` 完全一致（直接对比，不硬编码字面量）
   - 空动作（0 帧 / 全是空白帧）→ 报错，不出空图集
5. **新增 C# 用例（回归级）**：拿 `Misaka` 的 19 个 `sequence.json` 做数据源，
   断言生成出的条数 == 各动作在 Unreal 侧的 sprite 数
   （Click 4 / Death 8 / Defeat 1 / Ko 31 / Sk2 23 / Victory 19 / …，见 §4.2 表）。
   这一条是**把实机验证固化成测试**，将来改坏了一眼看得见。
6. **新增 C# 用例**：report 读取
   - 文件不存在 → 失败（不是「默认尺寸」）
   - 文件过期（早于本轮开始时刻）→ 失败
   - `ok:false` → 失败
7. **新增 C# 用例**：Python 定位（用一个假的探测结果注入，不真跑进程）
   - 设置项有值 → 用设置项的
   - 设置项为空 → 用内置的
   - 内置目录被挪走 → 回退到系统 Python
   - 全都没有 → 报错，且错误文案里含「去哪里配」
8. **棘轮**：`Tests/Program.cs:5244` 那 5 个数字若因此上升，要说明理由。

---

## 七、已定项（方案已收敛，无待拍板）

### 7.1 已定：两套实现全搬，`D:\图集\` 之后删掉

你原话：「**全搬，一个程序怎么能分开两个下载，丢个文件直接用不了了。**」
以及「把功能全部搬进去之后我就打算删除源文件了」—— 按这个办，见 §5.1 与 §5.5。

⚠️ 一旦 `D:\图集\` 删了，**这里就是唯一的源码副本**，改装箱算法必须在这个仓库里改、
跑这里的自检。所以搬的那一步要一次搬干净（含 `MANIFEST.md` / `README.md` /
`INTEGRATION.md` / `examples/` / `demo/` 素材），**不要留「一部分还在 D 盘」的中间态**。

### 7.2 已定：Node 侧一起搬，但工具箱不调它

见 §5.5。理由同上：**一个程序不能分两半**。搬过来放着、自检留着，
但工具箱不依赖 Node 运行时（主流程走 Python）。

### 7.3 已定：空白帧不出图

你回「**空白帧不算，我真的记得制作过了**」—— 以实机为准，见 §4.2 的验证表：
**所有 19 个动作的 UE sprite 数都和工具箱清单帧数完全相等，一个空白帧都没找到。**
按「空白帧不进图集、但序号不塌陷」办。

顺便说明：我上一版拿「空白帧要占位」当结论，是根据代码里 `imported.append(None)`
这条分支推的。代码确实支持空白帧，但**你的素材里目前没有用**，所以这个分支在图集侧
按「不出图」处理就够了 —— 真出现时也不会错（那一帧不产出像素，序号照留）。

### 7.4 已定：**内置 Python**（已实测跑通）

你回「**内置一个吧，也不大**」—— 实测**确实不大**：进 git 只多 **17.7MB**
（解压后 35.2MB），跑完 55 项自检 + 端到端出图都通过。

见 §5.4，其中有**一个实测踩到的坑必须记住**：embeddable 的 `python313._pth`
默认只有两行，**不加 `Lib/site-packages` 这一行，PIL 永远导不进来**。
这个坑在「解压看起来正常、一跑就 ModuleNotFoundError」时才暴露。

### 7.5 已定：**不处理**「有些序列甚至没有帧」

你回「**那就先不考虑这种情况，因为真的要改也不是图集生成相关的事情，
之后我遇到了会在插件那边想办法。**」

按这个办：图集清单**按「一帧一条」生成**，不为此加任何特例分支。
理由（也记进方案，将来别人看得到）：那是同步/插件侧的事，图集只是消费清单，
不该为了上游的一种异常形态去扭曲自己的契约。

> 这一条我先前标成「待澄清」，现在明确**关闭** —— 留着也会让后面读方案的人以为有坑。

---

## 八、落地顺序建议

分三步，每步都能独立验：

1. **搬工具 + 内置 Python + 跑通进程调用**（不改界面）
   搬 `Tools/Atlas/` → 内置解释器 + Pillow 解压就位 → 三条自检跑通
   → 加 `AtlasPackService` → 加设置项 → 写 C# 用例直接调服务
2. **接 St5 导出区**（场景 A，用户可见）
   加按钮 → 落 `Export/` → 显示尺寸
3. **接序列同步**（场景 B，自动）
   落 `tool/AtlasCache/` → 加清理 → 同步流程内调用

先做 1，确认「进程能起来、report 能读回」这件事通了，后面两步都是接线。

> ⚠️ 第 1 步里有个**容易漏的收尾**：确认 `Pakout.ps1` 打包时会带上 `Tools/Atlas/python/`
> （内置解释器是相对**可执行文件**的路径，不是相对源码目录）。这一步不做，
> 本地跑得好好的，打出来的包一导出就报「找不到 Python」。

---

## 九、明确不做

- **不做 C# 重写**（理由见 §2）
- **不让工具箱知道装箱参数** —— padding/extrude/rotate/maxSize 一律用图集工具的默认值。
  你原话「保存现在的默认就很好了」，那就**不暴露这些设置**，需要时再开。
- **不自动把图集导进 Unreal** —— 那是「序列同步」的职责，图集只是它的原料之一。
- **不压缩帧序号换空间** —— 见 §4.2，序号是资产名的组成部分，错了会静默建重资产。
- **不把复用帧合并成一条** —— 见 §4.2 实机验证，UE 侧每个帧位都是独立 sprite，
  合并会让 Flipbook 少帧。图集里同一张图可以只存一份像素，但**清单必须一条不少**。
- **不为「零帧序列」加特例分支** —— 见 §7.5，那是插件侧的事。
